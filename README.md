# Flowdesk

> B2B approval & workflow admin platform. Portfolio showcase project.

Work in progress.

## Repo structure

```
flowdesk/
├── frontend/   Vite + React + TypeScript app (not yet scaffolded)
├── backend/    Express + TypeScript + Prisma API
├── shared/     TS types shared between frontend/backend
├── PLAN.md
└── README.md
```

## Running locally

Backend setup instructions live in [backend/README.md](./backend/README.md) once added; for now see `backend/.env.example` for required environment variables.
# flow-desk
